#include "includes.h"
#ifndef JSON_UTILS
#define JSON_UTILS
int get_account(cJSON *json_obj,
                const char *list[],
                int number_of_inf,
                size_t maxlen);

#endif // DEBUG
