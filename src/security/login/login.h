#include "includes.h"

#ifndef LOGIN
#define LOGIN
int login(const char *users_files,const char *input_username,const char *input_password,size_t username_maxlen,size_t password_maxlen);

#endif // !LOGIN
